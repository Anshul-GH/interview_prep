# JD2 POC: Ollama + FastAPI + LangChain RAG + ChromaDB
# Python 3.12 compatible - lets pip resolve versions

fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.32.0

# LangChain ecosystem (latest compatible)
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-core>=0.3.10,<0.4.0
langchain-ollama>=0.2.0

# Local vector DB
chromadb>=0.5.0

# Utils (no heavy Rust)
python-dotenv>=1.0.0
httpx>=0.27.0
